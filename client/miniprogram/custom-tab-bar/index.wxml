<t-tab-bar t-class="t-tab-bar" value="{{value}}" bind:change="onChange" split="{{false}}" theme="tag">
    <t-tab-bar-item style="--td-tab-bar-height: 100rpx; padding: 0;" wx:for="{{list}}" wx:key="index" value="{{item.value}}" icon="{{item.icon}}">{{item.label}}</t-tab-bar-item>
</t-tab-bar>