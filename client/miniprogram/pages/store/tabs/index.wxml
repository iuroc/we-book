<t-tabs t-class="t-tabs" value="{{activeID}}" space-evenly="{{false}}" bind:change="onTabsChange">
    <t-tab-panel wx:for="{{categories}}" wx:key="index" label="{{item.name}}" value="{{item.id}}" />
</t-tabs>